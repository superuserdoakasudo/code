/*CSS (Cascading Style Sheets) is a stylesheet language used for describing the look and formatting of a document written in HTML or XML. It cannot perform computations or take input from users like a programming language. Therefore, it is not possible to write the requested program in CSS.

If you want to create a web-based application that converts decimal numbers to Roman numerals, you can use HTML, CSS, and JavaScript together. HTML would be used for the structure of the page, CSS for styling, and JavaScript for the functionality.

Here's an example of how you could create a simple web application using HTML, CSS, and JavaScript: */

index.html:

html
Copy code
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Decimal to Roman Converter</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="container">
        <h1>Decimal to Roman Converter</h1>
        <label for="decimal-input">Enter a decimal number (1-3999):</label>
        <input type="number" id="decimal-input" min="1" max="3999">
        <button id="convert-btn">Convert</button>
        <p id="result"></p>
    </div>
    <script src="script.js"></script>
</body>
</html>
styles.css:

css
Copy code
body {
    font-family: Arial, sans-serif;
    background-color: #f0f0f0;
}

.container {
    max-width: 600px;
    margin: 50px auto;
    padding: 20px;
    background-color: white;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    text-align: center;
}

label, input, button {
    display: block;
    margin: 10px 0;
}

button {
    padding: 5px 20px;
    background-color: #007BFF;
    color: white;
    border: none;
    cursor: pointer;
}

button:hover {
    background-color: #0056b3;
}

#result {
    font-weight: bold;
    margin-top: 20px;
}
script.js:

javascript
Copy code
document.getElementById("convert-btn").addEventListener("click", function () {
    const decimalInput = document.getElementById("decimal-input");
    const result = document.getElementById("result");
    const decimalNumber = parseInt(decimalInput.value, 10);

    if (1 <= decimalNumber && decimalNumber <= 3999) {
        const romanNumeral = decimalToRoman(decimalNumber);
        result.textContent = `Roman numeral representation of ${decimalNumber} is ${romanNumeral}`;
    } else {
        result.textContent = "Invalid input! Please enter a number between 1 and 3999.";
    }
});

function decimalToRoman(num) {
    const val = [1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1];
    const symbols = ["M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"];
    let romanNumeral = "";
    let i = 0;

    while (num > 0) {
        while (num >= val[i]) {
            num -= val[i];
            romanNumeral += symbols[i];
        }
        i++;
    }
